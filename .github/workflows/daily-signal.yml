name: Daily TQQQ Signal
on:
  schedule:
    - cron: "30 22 * * 1-5"
    # Recovery fetch before the next US open: 09:00 EDT / 08:00 EST.
    # Tue-Sat UTC corresponds to the morning after each Mon-Fri US session.
    - cron: "0 13 * * 2-6"
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "app/**"
      - "lib/**"
      - "github-pages/main.tsx"
      - "github-pages/index.html"
      - "github-pages/public/sw.js"
      - "github-pages/public/manifest.webmanifest"
      - "vite.pages.config.ts"
      - "package.json"
      - "package-lock.json"
      - "tests/**"
      - ".github/workflows/daily-signal.yml"
      - "github-pages/public/data/deep-research.json"
      - "github-pages/public/data/cross-ticker.json"
      - "github-pages/public/data/native-research.json"
      - "github-pages/public/data/ticker-forward-ledger.json"
      - "github-pages/public/data/ticker-forward-status.json"
      - "github-pages/public/data/production-config.json"
permissions:
  contents: write
  pages: write
  id-token: write
concurrency:
  group: daily-signal-pages
  cancel-in-progress: false
jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Fetch data and generate signal
        run: npm run generate:daily
      - name: Run operational regression tests
        run: npm run test:ops
      - name: Save append-only live signal history
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add github-pages/public/data
          if ! git diff --cached --quiet; then
            git commit -m "Update daily TQQQ signal"
            git pull --rebase origin main
            git push
          fi
      - name: Build PWA from the persisted latest main
        run: npm run build:pages
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist-pages
      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
      - name: Mark data failure after publishing status
        run: test ! -s github-pages/public/data/.failed
