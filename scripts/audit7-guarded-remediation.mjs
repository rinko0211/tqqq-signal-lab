import fs from "node:fs";

const patch=(path,from,to)=>{let s=fs.readFileSync(path,"utf8");if(s.includes(to))return;if(!s.includes(from))throw Error(`Audit 7 patch guard failed: ${path}`);s=s.replace(from,to);fs.writeFileSync(path,s)};

// M03 — user-facing authority bundle coherence.
patch("app/page.tsx",'import {nyseExecutionWindow} from "../lib/market-calendar";','import {nyseExecutionWindow} from "../lib/market-calendar";\nimport {operationalAuthorityBundleIsCoherent} from "../lib/operational-authority";');
patch("app/page.tsx",'    authorityUnsafe=Boolean(!dailySignal||!runtimeStatus||!productionConfigIsValid(productionConfig)||!forwardLedger),','    authorityBundle=operationalAuthorityBundleIsCoherent({signal:dailySignal,status:runtimeStatus,production:productionConfig,forward:forwardLedger}),\n    authorityUnsafe=!authorityBundle.ok,');

// M04 — Forward internal integrity + explicit prior-anchor proof.
patch("lib/forward.ts",
'export function updateForwardLedger(ds: Dataset, input: ForwardLedger | null | undefined, source: string, generatedAt = new Date().toISOString()) {\n  if(input&&(input.schemaVersion!==FORWARD_SCHEMA_VERSION||input.appendOnly!==true))throw Error("Forward prior ledger is invalid; refusing append-only reset");\n  const ledger = input ? structuredClone(input) : emptyForwardLedger(generatedAt);\n  assertForwardFreezeIntegrity(ledger);',
'export function assertForwardLedgerInternalIntegrity(ledger:ForwardLedger){\n  if(ledger.schemaVersion!==FORWARD_SCHEMA_VERSION||ledger.appendOnly!==true)throw Error("Forward prior ledger is invalid; refusing append-only reset");\n  assertForwardFreezeIntegrity(ledger);\n  const keys=new Set<string>(),logical=new Set<string>();\n  for(const r of ledger.records){\n    const freeze=ledger.freezes.find(f=>f.version===r.strategyVersion);if(!freeze)throw Error(`Forward integrity: unknown strategy version ${r.strategyVersion}`);\n    const expected=keyOf(r.strategyVersion,r.marketDataDate);if(r.key!==expected)throw Error(`Forward integrity: record key mismatch ${r.key}`);\n    if(r.marketDataDate<freeze.startDate)throw Error(`Forward integrity: pre-start record ${r.key}`);\n    if(keys.has(r.key)||logical.has(expected))throw Error(`Forward integrity: duplicate record ${r.key}`);keys.add(r.key);logical.add(expected);\n  }\n}\n\nexport function updateForwardLedger(ds: Dataset, input: ForwardLedger | null | undefined, source: string, generatedAt = new Date().toISOString()) {\n  if(input)assertForwardLedgerInternalIntegrity(input);\n  const ledger = input ? structuredClone(input) : emptyForwardLedger(generatedAt);\n  assertForwardLedgerInternalIntegrity(ledger);');
patch("lib/forward.ts",
'  const startIndex = last ? ds.days.findIndex(d => d.date === last.marketDataDate) + 1 : ds.days.findIndex(d => d.date >= freeze.startDate);\n  if (startIndex < 0) return;',
'  const anchorIndex=last?ds.days.findIndex(d=>d.date===last.marketDataDate):-1;\n  if(last&&anchorIndex<0)throw Error(`Forward prior anchor missing from provider dataset: ${freeze.version} ${last.marketDataDate}`);\n  const startIndex = last ? anchorIndex + 1 : ds.days.findIndex(d => d.date >= freeze.startDate);\n  if (startIndex < 0) return;');

// M04 — Phase 5 internal integrity + explicit per-system prior-anchor proof.
patch("lib/phase5-forward.ts",
'function assertPhase5FreezeIntegrity(ledger:Phase5Ledger){for(const frozen of PHASE5_FREEZES){const existing=ledger.freezes.find(x=>x.version===frozen.version);if(!existing)throw new Error(`Phase 5 freeze missing: ${frozen.version}`);if(JSON.stringify(existing)!==JSON.stringify(frozen))throw new Error(`Phase 5 freeze drift blocked: ${frozen.version}`)}if(ledger.freezes.length!==PHASE5_FREEZES.length)throw new Error("Phase 5 unexpected freeze definition")};\nexport function updatePhase5LedgerSubset(payload:Payload,input:Phase5Ledger|null|undefined,versions:string[],generatedAt=new Date().toISOString()):Phase5Ledger{\n  if(input&&(input.schemaVersion!==1||input.appendOnly!==true))throw new Error("Phase 5 prior ledger is invalid; refusing append-only reset");\n  const ledger=input?structuredClone(input):emptyPhase5Ledger(generatedAt);assertPhase5FreezeIntegrity(ledger);const wanted=new Set(versions);',
'function assertPhase5FreezeIntegrity(ledger:Phase5Ledger){for(const frozen of PHASE5_FREEZES){const existing=ledger.freezes.find(x=>x.version===frozen.version);if(!existing)throw new Error(`Phase 5 freeze missing: ${frozen.version}`);if(JSON.stringify(existing)!==JSON.stringify(frozen))throw new Error(`Phase 5 freeze drift blocked: ${frozen.version}`)}if(ledger.freezes.length!==PHASE5_FREEZES.length)throw new Error("Phase 5 unexpected freeze definition")};\nexport function assertPhase5LedgerInternalIntegrity(ledger:Phase5Ledger){\n  if(ledger.schemaVersion!==1||ledger.appendOnly!==true)throw new Error("Phase 5 prior ledger is invalid; refusing append-only reset");assertPhase5FreezeIntegrity(ledger);\n  const keys=new Set<string>(),logical=new Set<string>();for(const r of ledger.records){const freeze=ledger.freezes.find(f=>f.version===r.strategyVersion);if(!freeze)throw new Error(`Phase 5 integrity: unknown version ${r.strategyVersion}`);const expected=keyOf(r.strategyVersion,r.marketDataDate);if(r.key!==expected)throw new Error(`Phase 5 integrity: record key mismatch ${r.key}`);if(r.marketDataDate<freeze.startDate)throw new Error(`Phase 5 integrity: pre-start record ${r.key}`);if(keys.has(r.key)||logical.has(expected))throw new Error(`Phase 5 integrity: duplicate record ${r.key}`);keys.add(r.key);logical.add(expected);}\n}\nexport function updatePhase5LedgerSubset(payload:Payload,input:Phase5Ledger|null|undefined,versions:string[],generatedAt=new Date().toISOString()):Phase5Ledger{\n  if(input)assertPhase5LedgerInternalIntegrity(input);\n  const ledger=input?structuredClone(input):emptyPhase5Ledger(generatedAt);assertPhase5LedgerInternalIntegrity(ledger);const wanted=new Set(versions);');
patch("lib/phase5-forward.ts",
'  const startIndex=last?ds.days.findIndex(d=>d.date===last.marketDataDate)+1:ds.days.findIndex(d=>d.date>=freeze.startDate);\n  if(startIndex<0)return;',
'  const anchorIndex=last?ds.days.findIndex(d=>d.date===last.marketDataDate):-1;\n  if(last&&anchorIndex<0)throw new Error(`Phase 5 prior anchor missing from provider dataset: ${freeze.version} ${last.marketDataDate}`);\n  const startIndex=last?anchorIndex+1:ds.days.findIndex(d=>d.date>=freeze.startDate);\n  if(startIndex<0)return;');

// M04 — Lifecycle event integrity.
patch("lib/lifecycle-review.ts",
'export function updateLifecycleReview(args:{phase5:Phase5Ledger;forward:ForwardLedger;production:ProductionConfig;phase5Status?:LifecycleInputStatus|null;runtimeStatus?:RuntimeStatus|null;prior?:LifecycleLedger|null;now?:string}):LifecycleLedger{\n  const now=args.now??new Date().toISOString(),asOf=marketDate(now);if(!DATE_RE.test(asOf))throw Error("Invalid lifecycle review date");\n  if(args.prior&&(args.prior.schemaVersion!==1||args.prior.appendOnly!==true))throw Error("Lifecycle prior ledger is invalid; refusing append-only reset");',
'export function assertLifecycleLedgerInternalIntegrity(ledger:LifecycleLedger){\n  if(ledger.schemaVersion!==1||ledger.appendOnly!==true)throw Error("Lifecycle prior ledger is invalid; refusing append-only reset");const keys=new Set<string>();for(const e of ledger.events){if(!e.key||keys.has(e.key))throw Error(`Lifecycle integrity: duplicate/invalid event key ${e.key}`);if(!DATE_RE.test(e.reviewDate)||!Number.isFinite(Date.parse(e.recordedAt)))throw Error(`Lifecycle integrity: invalid event chronology ${e.key}`);keys.add(e.key);}\n}\nexport function updateLifecycleReview(args:{phase5:Phase5Ledger;forward:ForwardLedger;production:ProductionConfig;phase5Status?:LifecycleInputStatus|null;runtimeStatus?:RuntimeStatus|null;prior?:LifecycleLedger|null;now?:string}):LifecycleLedger{\n  const now=args.now??new Date().toISOString(),asOf=marketDate(now);if(!DATE_RE.test(asOf))throw Error("Invalid lifecycle review date");\n  if(args.prior)assertLifecycleLedgerInternalIntegrity(args.prior);');

// M04 — Production Health event integrity, checked before inactive early-return.
patch("lib/production-health-review.ts",
'export function updateProductionHealthLedger(args:{production:ProductionConfig;lifecycle:LifecycleLedger;prior?:ProductionHealthLedger|null;now?:string}):ProductionHealthLedger{\n  const now=args.now??new Date().toISOString(),asOf=marketDate(now);if(args.prior&&(args.prior.schemaVersion!==1||args.prior.appendOnly!==true))throw Error("Production Health prior ledger is invalid; refusing append-only reset");const out=args.prior?structuredClone(args.prior):emptyProductionHealthLedger(now),p=args.production;',
'export function assertProductionHealthLedgerInternalIntegrity(ledger:ProductionHealthLedger){\n  if(ledger.schemaVersion!==1||ledger.appendOnly!==true)throw Error("Production Health prior ledger is invalid; refusing append-only reset");const keys=new Set<string>(),logical=new Set<string>();for(const e of ledger.events){const id=`${e.version}|${e.dueDate}`;if(!e.key||e.key!==id||keys.has(e.key)||logical.has(id))throw Error(`Production Health integrity: duplicate/invalid event ${e.key}`);if(!Number.isFinite(Date.parse(e.recordedAt)))throw Error(`Production Health integrity: invalid recordedAt ${e.key}`);keys.add(e.key);logical.add(id);}\n}\nexport function updateProductionHealthLedger(args:{production:ProductionConfig;lifecycle:LifecycleLedger;prior?:ProductionHealthLedger|null;now?:string}):ProductionHealthLedger{\n  const now=args.now??new Date().toISOString(),asOf=marketDate(now);if(args.prior)assertProductionHealthLedgerInternalIntegrity(args.prior);const out=args.prior?structuredClone(args.prior):emptyProductionHealthLedger(now),p=args.production;');

// Permanent registration of Audit 7 behavioral/fault probes.
const packagePath="package.json",pkg=JSON.parse(fs.readFileSync(packagePath,"utf8"));
for(const key of ["test:core","test:ops"]){const marker="tests/audit6-final-failclosed.test.mjs",addition="tests/audit7-state-space.test.ts tests/audit7-authority-bundle.test.ts tests/audit7-ledger-integrity.test.ts";if(typeof pkg.scripts?.[key]!=="string"||!pkg.scripts[key].includes(marker))throw Error(`Audit 7 package guard failed: ${key}`);if(!pkg.scripts[key].includes("tests/audit7-state-space.test.ts"))pkg.scripts[key]=pkg.scripts[key].replace(marker,`${marker} ${addition}`)}
fs.writeFileSync(packagePath,JSON.stringify(pkg,null,2)+"\n");
